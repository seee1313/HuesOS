[package]
name = "huesos-shutdown-broker"
version = "0.1.0"
edition = "2021"
license = "MIT"

[[bin]]
name = "huesos-shutdown-broker"
path = "src/main.rs"

[dependencies]
libcanvas = { path = "../libcanvas" }
huesos-abi = { path = "../../huesos-abi" }

[profile.release]
opt-level = 2
panic = "abort"
lto = false

[profile.dev]
panic = "abort"

[workspace]
